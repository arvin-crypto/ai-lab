# LLM Provider: "ollama" (local, default) or "gemini" (cloud)
LLM_PROVIDER=ollama

# Only needed if LLM_PROVIDER=gemini
# Get your key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-key-here
